@model IEnumerable<IMS_Dashboard.ViewModels.UnitOfMeasureVM.DisplayUnitViewModel>

@{
    ViewData["Title"] = "Unit Of Measure List";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">@ViewData["Title"]</h4>
            @* <p class="card-description">
            Add class <code>.table-bordered</code>
            </p> *@
            <div class="table-responsive pt-3">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th> # </th>
                            <th> @Html.DisplayNameFor(model => model.UnitName) </th>
                            <th> @Html.DisplayNameFor(model => model.Abbreviation) </th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null)
                        {
                            int rowNumber = 1;
                            @foreach (var units in Model)
                            {
                                <tr>
                                    <td> @rowNumber </td>
                                    <td> @units.UnitName </td>
                                    <td> @units.Abbreviation </td>
                                </tr>
                                rowNumber++;
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

